# Систем асалт мониторинг + Авто сэргээлт — Тохиргооны заавар

Энэ заавар нь ERP системийн **асалттай эсэхийг гар утаснаас мэдэх**, **унтарсан/сэргэсэн үед Telegram мэдэгдэл авах**, мөн **тог/интернет тасраад ирэхэд систем өөрөө сэргэдэг** болгох тохиргоог алхам алхмаар тайлбарлана.

> **Гол зарчим:** Систем өөрийнхөө унтарсныг өөрөө мэдэгдэж чадахгүй (тог/интернетгүй болно). Тиймээс гадна талд байрлах үнэгүй "харуул" (**Healthchecks.io**) ERP-ийн heartbeat-ийг хүлээж авахаа болиход танд Telegram-аар мэдэгдэнэ.

Код тал (heartbeat loop, auto-restart wrapper) аль хэдийн бэлэн. Доорх нь **нэг удаагийн тохиргоо**.

---

## A. Telegram bot үүсгэх (~5 мин)

1. Telegram дээр **@BotFather**-ыг нээж `/newbot` бичнэ. Нэр өгөөд **bot token** авна (`123456:ABC-...` хэлбэртэй).
2. Мэдэгдэл авах хүмүүсээ багтаасан **групп** үүсгэж, дээрх bot-оо групп-т нэмнэ.
3. Групп-ийн **chat_id** авах:
   - Групп дотор ямар нэг мессеж бичнэ.
   - Браузераар нээх: `https://api.telegram.org/bot<TOKEN>/getUpdates`
   - Хариултаас `"chat":{"id":-100xxxxxxxxxx,...}` доторх **id**-г авна (групп бол сөрөг тоо).
   - (Хялбар арга: групп-т **@RawDataBot** нэмээд chat id-г шууд харуулна, дараа нь хасна.)

---

## B. Healthchecks.io "харуул" тохируулах (~5 мин)

1. [healthchecks.io](https://healthchecks.io) дээр **үнэгүй** бүртгүүлнэ.
2. **Add Check** → нэр: `Buten-Orgil ERP`.
3. Тохиргоо:
   - **Period** = `1 minute` (ERP минут тутам ping илгээнэ)
   - **Grace Time** = `5 minutes` (богино тасалдалд хуурамч дохио өгөхгүй — 5 мин ping ирэхгүй бол л DOWN)
4. Check-ийн **Ping URL**-г хуулна (`https://hc-ping.com/<uuid>` хэлбэртэй). → Part C-д ашиглана.
5. **Integrations** → **Telegram** → зааврыг дагаж өөрийн Telegram-тай холбоно (эсвэл A хэсгийн групп-т Healthchecks-ийн ботыг нэмнэ). Ингэснээр DOWN/UP мэдэгдэл **PC-ээс хамааралгүйгээр** Telegram-д ирнэ.

**Үр дүн:**
- PC/интернет/процесс унасан → ping зогсоно → 5 мин дараа Telegram **🔴 DOWN**.
- Сэргэсэн → ping сэргэнэ → Telegram **🟢 UP** + ERP-ийн "✅ аслаа" мессеж.
- **Шууд шалгах:** Healthchecks-ийн status хуудсыг утсандаа bookmark хийж хэзээ ч нээгээд одоогийн төлөв харна.

---

## C. `backend\.env` бөглөх

`backend\.env` файлыг нээж дараах мөрүүдийг бодит утгаар солино (энэ файл git-д ордоггүй, аюулгүй):

```
HEARTBEAT_URL=https://hc-ping.com/<таны-uuid>
HEARTBEAT_INTERVAL_SEC=60
TELEGRAM_BOT_TOKEN=<BotFather-аас авсан token>
TELEGRAM_CHAT_ID=<групп-ийн chat id>
```

> `HEARTBEAT_URL` хоосон бол мониторинг **идэвхгүй** (юу ч илгээхгүй) — тохиргоо дуустал систем хэвийн ажиллана.

Дараа нь `startup.bat`-ыг дахин ажиллуулж backend-ийг restart хийнэ. Backend-ийн цонхонд `[heartbeat] мониторинг идэвхтэй` гарч, ~60с тутам `[heartbeat] ok` гарна. Healthchecks ногоон болж, Telegram-д "аслаа" ирнэ.

---

## D. Авто сэргээлт (тог/интернет сэргэхэд өөрөө сэргэх)

### D1. BIOS — тог ирэхэд PC өөрөө асна ⭐ (хамгийн чухал)
Компьютерийг унтрааж, асаах үед **DEL/F2** дараад BIOS/UEFI руу орно. **Power / APM** хэсгээс:
- **"Restore on AC Power Loss"** (эсвэл "AC Power Recovery" / "After Power Failure" / "AC Back") = **Power On** (эсвэл "Last State").
- Хадгалаад гарна (F10).

Ингэснээр тог тасраад ирэхэд PC **өөрөө асна**. Энэ бол scenario 2-ийн үндсэн шийдэл.

### D2. Windows авто-нэвтрэлт
Ачаалахад нууц үг асуулгүй desktop хүрдэг болгоно:
1. `Win+R` → `netplwiz` → Enter.
2. Хэрэглэгчээ сонгоод **"Users must enter a user name and password to use this computer"** дээрх галочкийг **авна** → Apply.
3. Нууц үгээ 2 удаа оруулна.

### D3. Ачаалахад ERP авто-асаах
`setup_autostart.bat`-ыг **Administrator-аар** нэг удаа ажиллуулна (баруун товч → Run as administrator). Энэ нь "ERP Startup" Task Scheduler task үүсгэж, нэвтэрсний ~30с дараа `startup.bat`-ыг автоматаар ажиллуулна.

> Урсгал: **Тог ирнэ → PC асна (D1) → авто-нэвтэрнэ (D2) → startup.bat автоматаар (D3) → систем ажиллана.**

### D4. Backend унтарвал өөрөө дахин асах (аль хэдийн хийгдсэн)
`startup.bat` одоо backend-ийг `run_server_loop.bat` дамжуулан асаадаг болсон — процесс ямар нэг шалтгаанаар унавал **3 секундын дараа өөрөө дахин асна**. Гараар зогсоохдоо `stop.bat` ажиллуулна (энэ нь дахин асаахыг зогсооно).

### D5. UPS (сонголт, техник хангамж)
Жижиг UPS (~600–1000VA) авбал:
- Богино тог тасалдалд систем унтрахгүй тэсвэрлэнэ.
- Урт тасалдалд цэвэр унтрааж, SQLite өгөгдлийг эвдрэлээс хамгаална.
Заавал биш ч өгөгдлийн аюулгүй байдалд санал болгоно.

---

## E. Бүрэн турших (verification)

| Симуляц | Хийх зүйл | Хүлээгдэх үр дүн |
|---|---|---|
| **Хэвийн** | `startup.bat` ажиллуулах | Backend цонхонд `[heartbeat] ok`; Healthchecks ногоон; Telegram "аслаа" |
| **Гараар унтраах** | `stop.bat` | ~5 мин дотор Telegram **DOWN** (дахин асахгүй) |
| **Тог тасралт** | PC-г салгах | Grace дотор **DOWN**. Залгах → PC өөрөө асна → авто-нэвтэрнэ → startup.bat → **UP** + "аслаа" |
| **Интернет тасралт** | Router-ийн WAN салгах (PC асаалттай) | heartbeat хүрэхгүй → **DOWN**. Холбох → **UP** |
| **Процесс унах** | Backend цонхны uvicorn-г Ctrl+C-гүйгээр таслах | 3с дараа өөрөө дахин асна |
| **Шууд шалгах** | Утаснаас Healthchecks status хуудас нээх | Одоогийн төлөв (up/down) харагдана |

---

## Troubleshooting

- **Telegram "аслаа" ирэхгүй:** `.env`-ийн token/chat_id зөв эсэх; bot групп-т нэмэгдсэн эсэх; backend restart хийсэн эсэх.
- **Healthchecks ногоон болохгүй:** `HEARTBEAT_URL` зөв эсэх; PC интернеттэй эсэх; backend цонхонд `[heartbeat]` мөр гарч байгаа эсэх.
- **DOWN мэдэгдэл хэт олон дахин ирнэ:** Healthchecks-ийн **Grace Time**-г уртасга (жишээ 10 мин).
- **Авто-асалт ажиллахгүй:** D1 (BIOS) хийгдсэн эсэх; D2 (авто-нэвтрэлт); Task Scheduler-т "ERP Startup" task байгаа эсэх (`schtasks /Query /TN "ERP Startup"`).

---

## Тэмдэглэл
- Тог vs интернет тасралтыг гаднаас төгс ялгах боломжгүй (хоёулаа heartbeat зогсооно). DOWN/UP дохио + систем өөрөө сэргэсэн эсэх нь хангалттай мэдээлэл өгнө.
- Токенууд зөвхөн `backend\.env`-д хадгалагдана (git-д орохгүй).
